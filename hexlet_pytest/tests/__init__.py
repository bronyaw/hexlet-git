
from collections import defaultdict


def get_index_sum(lst, target):
    elements_counter = {}
    for index, item in enumerate(lst):
        elements_counter[item] = index
    for 
print(get_index_sum([1, 0, 2 , 4, 9], 6))
    
        